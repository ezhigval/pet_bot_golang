ZELENIAL — Code Style & Engineering Rules
0. Философия (самое важное) 🧠

Читаемость важнее краткости
Код читают чаще, чем пишут. Если выбор между «умно» и «понятно» — выбираем понятно.

Явность важнее магии
Никаких неочевидных хаков, скрытых side-effect’ов и «оно само».

Простота важнее абстракций
Абстракции вводятся только тогда, когда есть реальная боль дублирования или роста.

Код — часть продукта
Плохой код = технический долг = стресс в будущем. Мы пишем код так, будто будем поддерживать его 5 лет.

1. Общие правила для всего проекта
   Именование

Имена говорят, что они делают

Запрещены имена:

data, temp, item, foo, bar, test123

Разрешены:

userRepository

calculateDailyLoad

encryptedPersonalPayload

Размер сущностей

Функция: до 30–40 строк

Метод: делает одну логическую вещь

Файл: одна ответственность

Пакет / модуль: одна предметная область

Комментарии

Комментарии объясняют “почему”, а не “что”

Если код невозможно понять без комментария — он написан плохо

Публичные методы обязаны иметь комментарий

2. Go (Backend) Code Style 🦫
   Структура проекта

cmd/ — только точки входа

internal/ — бизнес-логика (закрытая)

pkg/ — переиспользуемые утилиты

docs/ — архитектура, схемы, ADR

deploy/ — инфраструктура

Пакеты

Названия в нижнем регистре

Короткие, предметные:

user

finance

scheduler

Запрещено:

utils

helpers (слишком абстрактно)

Структуры

Структура = данные

Бизнес-логика — в сервисах, не в моделях

type User struct {
ID        uuid.UUID
Email     string
CreatedAt time.Time
}

Интерфейсы

Интерфейсы маленькие

Объявляются там, где используются, а не где реализуются

Названия по поведению:

UserRepository

NotificationSender

Ошибки

Ошибки никогда не игнорируются

Ошибка — это часть контракта

Ошибки оборачиваются контекстом:

return fmt.Errorf("failed to create user: %w", err)

Контекст

context.Context передаётся первым аргументом

Никаких context.Background() внутри бизнес-логики

3. Архитектурные правила Backend 🏗️
   Слои (жёстко)

Handler / Controller
HTTP / gRPC, валидация входа

Service
Бизнес-логика

Repository
Работа с БД / кэшем

Infrastructure
Kafka, Redis, внешние API

➡️ Слой не знает о слоях выше себя.

Запрещено

Handler → Repository (мимо сервиса)

Repository с бизнес-логикой

SQL внутри сервисов

4. Frontend (React) Code Style ⚛️
   Компоненты

Один файл — один компонент

Компонент делает одно

Большие компоненты дробятся

Именование

Компоненты — PascalCase

Хуки — useSomething

Переменные — camelCase

Состояние

Локальное состояние — в компоненте

Глобальное — через state manager

Никаких «протаскиваний» пропсов через 5 уровней

UI

Логика и отображение разделены

Минимум условного рендера внутри JSX

Сложная логика — в хуках

5. Работа с AI и данными 🤖
   Персональные данные

Хранятся отдельно

Всегда шифруются

Никогда не отправляются целиком в AI

Обезличивание

AI видит:

возрастной диапазон

паттерны

агрегированные данные

AI не видит:

имя

точный адрес

прямые идентификаторы

AI = ассистент, не бог

AI даёт рекомендации

Решение всегда за пользователем

Никаких медицинских или юридических диагнозов

6. Git & Workflow 🌿
   Ветки

main — только стабильный код

dev — активная разработка

feature/<name> — одна фича

fix/<name> — один фикс

Коммиты

Маленькие

Осмысленные

В настоящем времени:

Add user profile endpoint

Fix calendar sync bug

PR

Один PR — одна идея

Описание: что, зачем, как проверить

7. Тестирование 🧪

Бизнес-логика обязана иметь тесты

Тест проверяет поведение, а не реализацию

Нет теста — нет гарантии

8. Документация 📚

README объясняет:

зачем проект

как запустить

как внести вклад

Архитектура описывается до кода

Любое неочевидное решение фиксируется текстом

9. Главное правило проекта

Если через месяц тебе будет стыдно открыть этот код — он написан неправильно.